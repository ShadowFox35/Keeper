name: keeper
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  core:
    path: ./core
  domain:
      path: ./domain
  data:
    path: ./data
  navigation:
    path: ./navigation

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: 4.0.0
  analyzer: 6.4.1

flutter:
  uses-material-design: true

